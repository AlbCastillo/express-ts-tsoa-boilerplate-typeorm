import { model } from 'mongoose';

import { {{pascalCase name}}I, {{pascalCase name}}Schema } from './{{name}}.schema';

export default model<{{pascalCase name}}I>('{{pascalCase name}}', {{pascalCase name}}Schema);
